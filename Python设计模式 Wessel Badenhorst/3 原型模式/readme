原型模式:
    就其核心而言,原型模式就是一个clone()函数,这个函数接受一个对象作为输入参数并且会返回它的一个副本.
    需求:我们需要创建大量几乎相同的对象,它们之间只有一个或两个细微的差异

    在原型模式中,我们优先使用组合而非继承.

    原型模式所需的三个组成部分如下:
        1,客户端通过要求原型克隆其自身来创建一个新的对象
        2,原型声明一个用于克隆自己的接口
        3,具体原型实现用于克隆其自身的操作

    深拷贝与浅拷贝的对比copytest.py

