单例模式:
    它仅仅是获得全局状态的一种很好的方式,但是大多时候全局状态正是我们希望在编程时避免使用的.
        希望避免使用全局状态的原因之一就是,项目一部分代码可能会修改该全局状态.


    当项目有几个部分不影响代码的执行时,那么使用全局状态就是可接受的.
        例如:日志,缓存,负载均衡,路由映射
        在所有这些情况下信息都是单项流动的,并且单例实例本身就不会发生变化.整个程序都不会变更它,因而
        不会出现项目的一部分干扰到另一部分的危险情况