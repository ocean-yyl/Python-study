# 19，模块
"""
当使用import语句导入模块时，默认情况下，会按照一下顺序查找：
1，在当前目录（此python脚本文件所在目录）下查找
2，到PYTHONPATH(环境变量)下的每个目录查找
3，到Python的默认安装目录下查找

"""
#1,查看python中有哪些模块（标准模块和第三方模块）
print(help('modules'))

#2,查看python已安装了哪些第三方模块。在命令行窗口输入：pip list